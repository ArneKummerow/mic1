[ ] stack should show hex and dec. stack tab should show addresses/values of everything between the base of the stack and the top. if there are less entries than there is space (e.g. at the beginning of an execution), the bottom of the stack should be at the bottom of the tab. scrolling occurs only if there are more entries than can be shown in the height of the tab. if thats the case, the scrolling should follow the TOS, but only if the user has scrolled to the top. if the user has scrolled down to see the stack at some lower level, no scrolling up shall happen to show the new TOS

[ ] breakpoints in macro code make breakpoints in microcode, therefore setting a breakpoint in macrocode will set breakpoints everywhere, wehere the macro instruction is used. add possibility to set breakpoint only at that macro code instruction, instead of only having breakpoints in micro code
